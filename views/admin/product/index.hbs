<div class="text-right mb-3">
    <a href="/admin/product/create" class="btn btn-primary ">Thêm sách mới</a>
</div>
<table class="table" border-collapse="collapse">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Mã sách</th>
            <th scope="col">Tên sách</th>
            <th scope="col">Số lượng còn</th>
            <th scope="col">Đơn giá</th>
            <th scope="col">Hình ảnh</th>
            <th scope="col">Hành động</th>
        </tr>
        </thread>
    <tbody>
        {{#each products}}
        <tr>
            <td>{{code}}</td>
            <td><a href="/admin/product/{{_id}}/edit">{{title}}</a></td>
            <td>{{subtract quantity borrow}}</td>
            <td>{{price}}VND</td>
            <td> <img src="{{img}}" alt="" width="30px"></td>
            <td>
                <a href="/admin/product/{{_id}}" class="btn btn-warning">Sửa</a>
                <a href="/admin/product/delete/{{_id}}" class="btn btn-danger" data-toggle="modal"
                    data-target="#exampleModal">Xóa</a>
            </td>
        </tr>
        {{/each}}
    </tbody>
    <!-- Modal -->
    <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Xác nhận xóa</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    Bạn có chắc chắn xóa sản phẩm này? Thao tác không thể quay trở lại!
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Hủy</button>
                    <button type="button" class="btn btn-primary">OK</button>
                </div>
            </div>
        </div>
    </div>

</table>